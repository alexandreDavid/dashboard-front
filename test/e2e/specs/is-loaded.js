const util = require('../util/util')

module.exports = {
  // beforeEach: function (browser) {
  //   util.beforeEach(browser)
  // },
  // 'go to auth': function (browser) {
  //   util.go2Auth(browser)
  //   browser.end()
  // },
  // 'is authenticated': function (browser) {
  //   util.authentifie(browser)
  //   browser.end()
  // }
}
